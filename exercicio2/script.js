const arrayNumbers = [1,2,3,4,5,6,7,8]

const arrayX3 = arrayNumbers.map((num) =>
{
    const triplos = num*3
    return triplos 
})

const arrayMetade = arrayNumbers.map((num) =>
{
    const metades = num/2
    return metades

})